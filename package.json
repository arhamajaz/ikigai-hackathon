{
  "name": "sentineledge",
  "version": "2.1.0",
  "description": "Zero-Latency Client-Side DLP Firewall Browser Extension",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/fix-firefox-manifest.js",
    "package": "npm run build && powershell -Command \"Compress-Archive -Path dist\\* -DestinationPath sentineledge-v2.1.0.zip -Force\"",
    "test": "npx tsx src/core/dlp-engine.test.ts && npx tsx src/core/semantic-ai.test.ts && npx tsx src/core/policy-engine.test.ts"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
